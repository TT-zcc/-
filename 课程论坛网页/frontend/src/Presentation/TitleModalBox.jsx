import { ModalBox } from '../dashboard/ModalBox'
import { styled } from '@mui/system';
// modal used to change the title
const TitleModalBox = styled(ModalBox)({
  height: '200px',
});

export default TitleModalBox;
