import { styled } from '@mui/system';
import Button from '@mui/material/Button';
// Buttons on the login and registration page
export const LoginRegisterButton = styled(Button)({
  maxWidth: '380px',
  width: '100%',
  height: '45px',
  marginBottom: '10px'
})
