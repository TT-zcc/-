import { styled } from '@mui/system';
// A form for entering content
export const Form = styled('form')({
  maxWidth: '380px',
  width: '100%',
  display: 'flex',
  alignItems: 'center',
  flexDirection: 'column'
})
