import { Link } from 'react-router-dom';
import { styled } from '@mui/system';
// Jump to the registration link style
export const GoToRegister = styled(Link)({
  width: '120px',
  color: 'blue',
});
