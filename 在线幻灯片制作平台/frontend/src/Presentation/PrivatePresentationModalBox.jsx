import { ModalBox } from '../dashboard/ModalBox'
import { styled } from '@mui/system';
// Container of modal
const PrivatePresentationModalBox = styled(ModalBox)({
  height: '600px',
});

export default PrivatePresentationModalBox;
