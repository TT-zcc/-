import { DashboardMainBox } from '../dashboard/DashboardMainBox';
import { styled } from '@mui/system';
// The container used to hold the entire private presentation page
export const ParticularPresentationMainBox = styled(DashboardMainBox)({
  flexDirection: 'row'
});

export default ParticularPresentationMainBox;
