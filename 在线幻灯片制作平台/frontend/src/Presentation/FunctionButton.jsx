import { styled } from '@mui/system';
import { HeadButton } from '../dashboard/HeadButton';
// Toolbar button
const FunctionButton = styled(HeadButton)({
  fontSize: '13px',
  height: '60px',
  marginLeft: '0',
  marginRight: '0',
  marginBottom: '30px'
})

export default FunctionButton;
