import styled from 'styled-components';
// The code block container on the slide
const NewCodeArea = styled.textarea`
  width: 100%;
  height: 100%;
  overflow: auto;
  border: 1px solid #ccc;
  box-sizing: border-box;
  font-family: Arial;
  font-size: inherit;
  border-radius: 4px;
  white-space: pre;
`;

export default NewCodeArea;
