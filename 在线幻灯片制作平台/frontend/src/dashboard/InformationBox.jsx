import { Box } from '@mui/material';
import { styled } from '@mui/system';
// A container for holding names, numbers of slides, and descriptions
export const InformationBox = styled(Box)({
  width: '100%',
  height: '40%'
});
